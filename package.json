{
  "scripts": {
    "update:about-my-github": "node scripts/update-about-github.mjs",
    "generate:images": "fish scripts/generate-images.fish",
    "render:profile": "node scripts/render-profile.mjs",
    "build": "npm run update:about-my-github && npm run generate:images && npm run render:profile",
    "daily:update-about-github": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "gitmoji-cli": "^9.7.0"
  }
}
